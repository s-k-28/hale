import Foundation

// Parity with the RN v1 App Store build (both OFF).
enum FeatureFlags {
    static let community = false      // EXPO_PUBLIC_COMMUNITY_ENABLED
    static let publicSquads = false   // EXPO_PUBLIC_PUBLIC_SQUADS_ENABLED
}
